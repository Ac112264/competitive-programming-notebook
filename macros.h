#ifndef MACROS_H
#define MACROS_H

#include <iostream>
#include <cmath>
#include <string>
#include <vector>
#include <list>
#include <set>
#include <map>
#include <queue>
#include <stack>
#include <algorithm>
#include <functional>

using namespace std; 

#define DEBUG(x) cerr << "> " << #x << " = " << x << '\n';
#define REPEAT(i,n) for(int i = 0; i < (n); i++)

#endif // MACROS_H
