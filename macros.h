#ifndef MACROS_H
#define MACROS_H

#include <iostream>
#include <cmath>
#include <string>
#include <vector>
#include <list>
#include <set>
#include <map>
#include <queue>
#include <stack>
#include <algorithm>
#include <functional>

using namespace std; 

#define debug_(x) cerr << "> " << #x << " = " << x << endl;
#define repeat_(i,n) for(int i = 0; i < (n); i++)

#endif
